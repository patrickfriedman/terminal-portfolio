let userConfig = undefined
try {
  userConfig = await import('./v0-user-next.config')
} catch (e) {
  // ignore error
}

/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    ignoreDuringBuilds: true,
  },
  typescript: {
    ignoreBuildErrors: true,
  },
  images: {
    unoptimized: true,
  },
  experimental: {
    webpackBuildWorker: true,
    parallelServerBuildTraces: true,
    parallelServerCompiles: true,
  },
}

mergeConfig(nextConfig, userConfig)

function mergeConfig(nextConfig, _userConfig) {
  if (!userConfig) {
    return
  }

  const resolvedUserConfig = _userConfig.default || _userConfig // Renamed to avoid shadowing
  for (const key in resolvedUserConfig) {
    if (
      typeof nextConfig[key] === 'object' &&
      !Array.isArray(nextConfig[key])
    ) {
      nextConfig[key] = {
        ...nextConfig[key],
        ...resolvedUserConfig[key],
      }
    } else {
      nextConfig[key] = resolvedUserConfig[key]
    }
  }
}

export default nextConfig
